{
    "layer": "top", // Waybar at top layer
    "height": 15, // Waybar height (to be removed for auto height)
    "modules-left": ["wlr/workspaces"],
    "modules-center": ["hyprland/window"],
    "modules-right": ["network","cpu","memory","temperature","clock","tray","custom/power"],
    "hyprland/window": {
        "format": "{}",
        "separate-outputs": true
    },
    "wlr/workspaces": {
         "format": "{icon}",
         "on-scroll-up": "hyprctl dispatch workspace e+1",
         "on-scroll-down": "hyprctl dispatch workspace e-1"
    },
    "clock": {
        "format": "{:%Y/%m/%d-%H:%M}",
    },
    "tray": {
        "icon-size": 21,
        "spacing": 10
    },
    "cpu": {
        "format": "CPU:{usage}%",
        "tooltip": false
    },
    "memory": {
        "format": "MEM:{used:0.1f}G/{total:0.1f}G"
    },
    "temperature": {
        "critical-threshold": 80,
        "format": "TEMP:{temperatureC}°C",
    },
    "network": {
        "format-wifi": "IP:{ipaddr}/{cidr}",
        "format-ethernet": "{ifname}: {ipaddr}/{cidr} ",
        "format-linked": "{ifname} (No IP) ",
        "format-disconnected": "Disconnected ⚠",
        "format-alt": "{essid} ({signalStrength}%)",
    },
    "custom/power": {
        "format": "",    
        "on-click": "swaynag -t warning -m 'Power Menu Options' -b 'Logout' 'swaymsg exit' -b 'Suspend' 'swaymsg exec systemctl suspend' -b 'shutdown' 'shutdown now'"
    }
}
